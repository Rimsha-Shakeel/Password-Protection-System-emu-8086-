# Password-Protection-System-emu-8086-
“A CLI-based password security system built in 8086 Assembly that encrypts user input, verifies credentials, limits login attempts, and locks the system after multiple failures to enhance basic authentication security.”
